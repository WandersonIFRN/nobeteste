<h1 style="display: inline">Articles</h1>
<%= link_to 'Novo', new_article_path, class: 'btn btn-secondary', style:"margin: 0 1em 1em 1em" %> <!-- chamar view para novo -->

<ul>
    <% @articles.each do |article| %> <!-- exibir cada objeto um a um -->
        <li>
        <!-- <a href="/articles/<%= article.id %>"><%= article.title %></a>-->  <!--link extenso -->
        <!-- <a href="<%= article_path(article) %>"><%= article.title %></a>-->  <!-- link resumo -->
        <%= link_to article.title, article , class: 'btn btn-secondary'%>  <!-- link usanso o "resouces" em rotas (config/routes.rb)-->
        <%= article.body %>
        <%= datacao(article.created_at) %>
        </li>
    <% end %>
</ul>
