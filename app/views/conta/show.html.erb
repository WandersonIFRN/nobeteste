<!--<p style="color: green"><%= notice %></p>-->
<h4>Conta <%= @contum.id %> </h4>
<%= render @contum %>

<div>
  <%= link_to "Edit this contum", edit_contum_path(@contum) %> |
  <%= link_to "Back to conta", conta_path %>

  <%= button_to "Destroy this contum", @contum, method: :delete %>
</div>
<br>
<%= link_to 'Nova Transação', new_article_path, class: 'btn btn-secondary', style:"margin: 0 1em 1em 1em" %> <!-- chamar view para novo -->
